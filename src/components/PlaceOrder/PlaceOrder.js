import React from 'react';
import image from "../../components/images/giphy.gif";

const PlaceOrder = () => {
    return (
        <div>
            <h1>Place order</h1>
            <img src={image} alt="" />
        </div>
    );
};

export default PlaceOrder;